/**
 * 观察者模式（六）
 * 
 * <pre>
 * 观察者模式与MVC
 * 
 * 在当前流行的MVC(Model-View-Controller)架构中也应用了观察者模式，
 * MVC是一种架构模式，它包含三个角色：模型(Model)，视图(View)和控制器(Controller)。
 * 其中模型可对应于观察者模式中的观察目标，而视图对应于观察者，控制器可充当两者之间的中介者。
 * 当模型层的数据发生改变时，视图层将自动改变其显示内容。
 * </pre>
 *
 * @author colg
 */
package cn.colg.example._06;